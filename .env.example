# Placeholders only — never commit real credentials.
# Populated in Phase 5 when Supabase Auth is configured.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
LOCAL_OWNER_EMAIL=
LOCAL_OWNER_PASSWORD=
OWNER_TIMEZONE=
